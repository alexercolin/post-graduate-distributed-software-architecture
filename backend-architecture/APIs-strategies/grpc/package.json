{
  "name": "grpc-api-demo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "proto:generate": "cd server && npm run proto:generate && cd ../client && npm run proto:generate",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
